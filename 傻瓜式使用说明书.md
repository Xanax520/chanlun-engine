# 缠论指标引擎 · 傻瓜式使用说明书

欢迎使用「缠论指标引擎」！本说明书面向**完全没接触过 DLL、公式、编译**的普通用户——不需要懂任何编程，跟着一步步做，就能在通达信或 MT4 上看到缠论结构图。

---

## 一、这东西是干什么的？

一句话：**在行情软件上自动画出缠论结构图**。

| 功能 | 图上长什么样 |
|------|-------------|
| 五级递归结构：分型 → 笔 → 线段 → 大段 → 高级段 | 彩色折线，逐级递归套叠 |
| 三级独立轨道：笔轨道 / 线段轨道 / 大段轨道 | 上轨、下轨平行线，跟随价格移动 |
| 大段中枢 | 矩形框（上沿 ZG、下沿 ZD、起止位置，只画第一个中枢） |
| 买卖点：二买、二卖、三买、三卖 | 图上直接标注文字 |
| 二三买选股公式 | 通达信条件选股：全市场 5220 只 × 30 分钟周期 × 5 天窗口 × 三级（笔、线段、大段），端到端约 13 秒（实测 12.52 秒）；同条件换 5 分钟周期（平均约 15000 根K线、一天窗口）约 36 秒 |

所有计算都在**你自己的电脑本地**完成，不联网、不上传任何数据，行情怎么走它就怎么画。

---

## 二、准备工作

1. 一台 Windows 电脑。
2. 行情软件：**通达信** 或 **MT4**（二选一即可，装哪个看哪边的）。
3. 下载发布包 `chanlun_engine_open_source.zip`（GitHub Releases 页面下载），解压到任意文件夹（例如 `D:\chanlun`）。

> 本项目由 AI 辅助打包发布，若部署遇到任何问题，请及时告知（GitHub Issues），会尽快处理。

解压后你会看到：

```
chanlun_engine_open_source/
├── dist/
│   ├── tdx/slzs_chanlun.dll          ← 通达信要用的 DLL（已编译好，直接用）
│   └── mt4/slzs_chanlun_mt4.dll      ← MT4 要用的 DLL（已编译好，直接用）
├── tdx_dll/formulas/缠论主图公式.txt  ← 通达信主图公式（用记事本打开复制）
├── tdx_dll/formulas/缠论二三买选股公式.txt ← 通达信选股公式（可选，用记事本打开复制）
├── mt4_dll/mql4/chanlun.mq4          ← MT4 指标源码（需要编译一次）
└── chanlun/ 等源码目录               ← 源代码，不用动
```

> 只装通达信的话，只需要 `dist\tdx\slzs_chanlun.dll` 和 `缠论主图公式.txt` 两个文件；
> 只装 MT4 的话，只需要 `dist\mt4\slzs_chanlun_mt4.dll` 和 `chanlun.mq4` 两个文件。

---

## 三、通达信（TDX）安装步骤

### 第 1 步：放 DLL

> ⚠️ **先「以管理员身份运行」**：如果你的通达信装在 `C:\Program Files` 等受保护目录，把 DLL 复制到 `T0002\dlls` 可能提示"权限不足/拒绝访问"，通达信也可能读不到自定义 DLL。建议先**右键通达信图标 → 「以管理员身份运行」**，并给 `T0002\dlls` 目录授予写入权限（或复制 DLL 时用管理员权限执行）。

1. 在桌面找到通达信图标，**右键 → 打开文件所在位置**（进入通达信安装目录）。
2. 进入 `T0002` 文件夹，打开 `dlls` 文件夹（若没有 `dlls` 文件夹就新建一个）。
3. 把发布包里的 `dist\tdx\slzs_chanlun.dll` **复制**进去。
4. **完全关闭通达信，重新打开**（DLL 只在软件启动时加载，不重启不生效）。

### 第 2 步：导入公式

1. 在通达信里按 **Ctrl + F** 打开「公式管理器」。
2. 【关键 ⚠️ 必做】先「**绑定 DLL**」：找到公式管理器里的“DLL函数”区域（各版本入口略不同，通常在底部按钮里），把 `slzs_chanlun.dll` 指定为 **第 3 号 DLL（DLL3）**。公式调用的是 `TDXDLL3()`，必须绑到 3 号槽；如果你的 dlls 目录里还有其它 DLL，更要点名绑到 3 号，否则 `TDXDLL3` 会调用到别的 DLL，图表就是空白。
3. 点「新建」，公式名称填 `缠论`，类型选「主图叠加」。
4. 用**记事本**打开 `缠论主图公式.txt`，全选复制（Ctrl+A → Ctrl+C）。
5. 回到公式管理器，把内容**粘贴**进公式编辑框（Ctrl+V）。
6. 点「确定」保存。

### 第 3 步：看图

1. 打开任意股票 / 期货 / 外汇的 K 线图。
2. 键盘输入 `缠论` 回车（或在主图上右键 → 主图指标 → 缠论）。
3. 图上即显示：五级折线、轨道、中枢矩形、买卖点文字。

### 第 4 步（可选）：二三买选股

1. Ctrl+F 打开「公式管理器」→「新建」，类型选「条件选股公式」，名称填 `缠论二三买选股`。
2. 用记事本打开 `缠论二三买选股公式.txt`，全选复制粘贴保存。
3. 通达信「功能 → 选股器 → 条件选股」，选该公式 → 执行选股。
4. 全市场 5220 只 × 30 分钟周期 × 5 天窗口，端到端约 **13 秒**出结果（实测 12.52 秒）；同条件换 5 分钟周期（平均约 15000 根K线、一天窗口）端到端约 **36 秒**（测试平台 i7-9700F / 8核 / 16GB / 通达信64位）。

---

## 四、MT4 安装步骤

### 第 1 步：放 DLL

1. 打开 MT4，菜单「文件 → 打开数据文件夹」。
2. 进入 `MQL4\Libraries` 文件夹。
3. 把发布包里的 `dist\mt4\slzs_chanlun_mt4.dll` **复制**进去。
4. **完全关闭 MT4，重新打开**。

### 第 2 步：放指标并编译（只需一次）

1. 再次「文件 → 打开数据文件夹」，进入 `MQL4\Indicators` 文件夹。
2. 把 `mt4_dll\mql4\chanlun.mq4` **复制**进去。
3. 回到 MT4，按 **F4** 打开 MetaEditor 编辑器。
4. 左侧导航栏找到 `Indicators\chanlun.mq4`，双击打开，按 **F7** 编译。
5. 编译成功（下方日志显示 `0 errors`）后，`MQL4\Indicators` 下会多出 `chanlun.ex4` 文件。

### 第 3 步：看图

1. 打开任意品种的图表。
2. 把左侧导航栏「技术指标 → 自定义 → chanlun」**拖到图表上**（或双击加载）。
3. 指标自动计算，图上出现五级折线、轨道、中枢矩形、买卖点文字。

> 提示：由于买卖点是由多级递归结构同构推出的，**建议把「最大K线数」调到 2500–5000 根**（菜单「图表 → 属性 → 常用」），数据太少会导致上层的线段、大段、高级段算不出来。

---

## 五、常见问题（FAQ）

**Q1：通达信图上什么都没有 / 全是空白？**

- 【最常见原因】检查是否**把 DLL 绑定到 3 号槽**（公式管理器 → “DLL函数” → 指定为 **第 3 号 DLL / DLL3**）。公式用的是 `TDXDLL3()`，若没绑定、或绑定到了别的槽，`TDXDLL3` 就调用不到本 DLL，图上自然是空白。若你的 dlls 目录里还有别的 DLL，务必把本 DLL 点名指定到 3 号槽。
- 检查 DLL 是否确实在 `T0002\dlls` 文件夹里。
- 若通达信装在 `C:\Program Files` 且以普通权限运行，DLL 可能读不到、或复制时提示权限不足 → 尝试**以管理员身份运行通达信**（右键图标 → 「以管理员身份运行」），并给 `T0002\dlls` 授予写入权限。
- 检查通达信是否**完全退出后重新打开**过（不是关窗口，是退出进程再启动）。
- 检查公式是否保存成功（Ctrl+F 里能看到「缠论」）。

**Q2：MT4 提示「无法加载 DLL」或指标显示不出来？**

- 确认用的是 `slzs_chanlun_mt4.dll`（32 位，MT4 不支持 64 位 DLL）。
- 确认 DLL 在 `MQL4\Libraries` 且 MT4 已完全重启。
- 检查 MT4 菜单「工具 → 选项 → 智能交易系统」：把「允许导入动态链接库」勾选上（部分平台默认关闭）。

**Q3：杀毒软件报警 / DLL 被删了？**

- 本 DLL 纯本地计算、不联网，可放心。被拦截时在杀毒软件里添加信任即可。
- 源码完全开源，你也可以自己编译一份（见 README「构建与测试」）。

**Q4：不同品种、不同周期，画出来的结构不一样？**

- 正常现象。缠论按 K 线结构计算，周期越长结构越稳定；每个品种、每个周期都是独立计算的。

**Q5：我想自己改参数 / 自己编译，可以吗？**

- 可以。整个项目 MIT 协议开源，自由使用、修改、商用，见 README。

**Q6：为什么只画一个中枢？**

- 当前版本只画第一个中枢，所有买点标记也围绕第一个中枢展开。需要第二、第三个中枢时，把源码和需求告诉 DeepSeek Harness 或 Qoder CN IDE 即可扩展（实现并不复杂）。

**Q7：用不惯「一个分型当笔」或「一笔当线段」，怎么关？**

- 这是笔的 Case3 与线段的 Case3/Case4 规则，默认开启。不习惯的朋友可以在 `chanlun/src/lib.rs` 中关闭后重新编译（具体改法见 README「自定义」一节）。

**Q8：分型的准确率多少？买点胜率多少？**

- 分型的准确率为 99.9999%，分型以上因人而异。作者只用新笔——重点不是笔本身，而是递归起来的结构稳定性更有参考价值。
- 递归的买点胜率：70%、60%、50%、40% 都有可能，评价标准不同，结果就不同。

**Q9：作者学缠论多久了？用缠论能盈利吗？**

- 10 年整。
- 用缠论能盈利吗？能，也不能。建议看看作者 6 年前发的文章《致死都不知道怎么死的"操盘手"》。

---

## 六、理论来源与作者

- 缠论理论来源于《缠论108课》；
- 三级轨道的理论来源为李晓军老师——三级轨道大有用处，不会的可以多看看李晓军老师的公开课；
- 所有算法均为作者独立开发，前后历时一年，其间有 AI 辅助开发。

## 七、版权说明

本项目以 **MIT 协议**开源，可自由使用、修改与商用，保留版权声明即可。详见仓库 LICENSE 文件。
